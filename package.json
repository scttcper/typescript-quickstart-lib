{
  "name": "--libraryname--",
  "version": "0.0.0-placeholder",
  "description": "",
  "author": "--username-- <--email-->",
  "license": "MIT",
  "repository": "",
  "keywords": [
    "typescript"
  ],
  "source": "src/index.ts",
  "main": "./--libraryname--.js",
  "module": "./--libraryname--.m.js",
  "typings": "./src/index.d.ts",
  "sideEffects": false,
  "scripts": {
    "lint": "tslint -t stylish -p .",
    "lint:fix": "tslint -p tsconfig.json -t stylish --fix 'src/**/*.ts' 'test/**/*.ts'",
    "prebuild": "rimraf dist",
    "build": "microbundle build --compress false --format cjs,es --target node -o dist",
    "build:docs": "typedoc --out docs --target es6 --theme minimal --mode file src && touch docs/.nojekyll",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:prod": "npm run test -- --coverage --no-cache",
    "travis-deploy-once": "travis-deploy-once",
    "presemantic-release": "cp package.json dist && cp LICENSE dist && cp README.md dist && rimraf dist/test",
    "semantic-release": "cd dist && semantic-release",
    "postinstall": "ts-node -O '{\"module\": \"commonjs\"}' setup.ts && npm install --ignore-scripts"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/inquirer": "0.0.43",
    "@types/jest": "23.3.5",
    "@types/lodash": "4.14.117",
    "@types/node": "10.11.7",
    "@types/rimraf": "2.0.2",
    "@types/shelljs": "0.8.0",
    "inquirer": "6.2.0",
    "jest": "23.6.0",
    "lodash": "4.17.11",
    "microbundle": "0.6.0",
    "prettier": "1.14.3",
    "replace-in-file": "3.4.2",
    "rimraf": "2.6.2",
    "semantic-release": "15.9.17",
    "shelljs": "0.8.2",
    "travis-deploy-once": "5.0.9",
    "ts-jest": "23.10.4",
    "ts-node": "7.0.1",
    "tslint": "5.11.0",
    "tslint-config-prettier": "1.15.0",
    "tslint-xo": "0.9.0",
    "typedoc": "0.13.0",
    "typescript": "3.1.3"
  },
  "release": {
    "branch": "--master--"
  },
  "jest": {
    "preset": "ts-jest",
    "collectCoverage": true
  },
  "engines": {
    "node": ">=8.0.0"
  }
}
